name: PR Title Check
description: Check that pull request titles contain specific message
inputs:
  pattern:
    description: "A regex pattern to check if a pull request title is valid."
    required: true
runs:
  using: node16
  main: dist/index.js
